{
  "name": "multiplayer-phaser-game",
  "private": true,
  "scripts": {
    "format": "prettier -w .",
    "build": "turbo run build"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "turbo": "^1.4.7"
  },
  "workspaces": [
    "game",
    "schema",
    "server"
  ]
}
